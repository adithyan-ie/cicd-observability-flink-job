# Copy to .env and fill in before running `docker compose up -d --build`.

# The data-layer EC2 instance's address (Kafka/Postgres/Grafana) — must
# match DATA_LAYER_HOST set in deploy/AWS/data-layer/.env on that instance.
DATA_LAYER_HOST=10.0.1.10
